// Elemek kiválasztása.
// @param selector: string a elemet kiválasztó szelektor.
function _q( selector ) {
    
    // Elemek kiválasztása.
    var elements = document.querySelectorAll( selector );
    
    return elements;
    
}